### Open terminal on the parent folder conversational_assistant_mcp and run the below command
``` bash
python -m conversational_assitant_mcp.auth.server
```

``` bash
py -m debugpy --listen 0.0.0.0:5678 -m conversational_assitant_mcp.auth.server
```